{
  "name": "s3t",
  "version": "1.0.0",
  "description": "Run npx s3t to shortify your npm commands or build command templates",
  "dependencies": {
  },
  "scripts": {
    "publish": "npm publish"
  }
}
